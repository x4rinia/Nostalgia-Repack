# Prerequisite installers

This folder contains the Microsoft Visual C++ Redistributable offline installers required by common server components:

- `VC_redist.x64.exe` for 64-bit components
- `VC_redist.x86.exe` for 32-bit components

Use the launcher's prerequisite check first. Install only the package it reports as missing.
